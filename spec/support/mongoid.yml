test:
  # mongoid 4
  sessions:
    default:
      database: mongoid_embedded_errors_test
      hosts:
        - db
  # mongoid 5 and newer
  clients:
    default:
      database: mongoid_embedded_errors_test
      hosts:
        - db
